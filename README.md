# Repro

Steps:

1. Install dependencies

```bash
npm install
```

2. Run the analyzer

```bash
npm run analyze
```

3. Observe the console.log output, it should contain references to `node_modules`.
